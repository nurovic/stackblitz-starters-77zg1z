{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "start": "nodemon FactoryPattern.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "nodemon": "^3.0.1"
  }
}
